# 1Password shell plugins: CLIs such as gh get their tokens from 1Password,
# after Touch ID. `op plugin init <cli>` writes the aliases to this file.
[[ -f ~/.config/op/plugins.sh ]] && source ~/.config/op/plugins.sh
