# Claude Code (CLI and the desktop app's Code tab).
# The Gmail connector turns every URL and domain in an email into a google.com
# redirect (anthropics/claude-code#66010); this blocks such sends and drafts.
claude hook PreToolUse mcp__.*__(send_message|create_draft|update_draft) no-links-in-email.sh
